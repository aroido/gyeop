# 구현 스펙

GitHub 이슈별 검토 완료 스펙을 `issue-<number>.md`로 저장한다.

- `scripts/task-harness spec <issue-number>`로 초안을 만든다.
- `$gyeop-spec-writer`로 내용을 구체화한다.
- 스펙 리뷰 P0/P1 발견 사항이 0일 때 `Status: Reviewed`로 바꾼다.
- 스펙은 구현과 함께 PR에 포함한다.
- QA 판정은 `docs/temp/qa/`에 두고 커밋하지 않는다.

